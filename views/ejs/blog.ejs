<%- include('header'); -%>

<section id="hero" class="d-flex align-items-center header1" style="background-color: var(--p-color); height: 100vh;">
  <div class="container">
    <div class="row">
      <div
        class="col-lg-6 pt-5 pt-lg-0 order-2 order-lg-1 d-flex flex-column justify-content-center"
      >
        <h1>Stay informed and inspired with the latest insights</h1>
        <h2>
          Discover a wealth of knowledge in our blog archives. Whether you're a novice or an experienced investor, there's always something new to learn and explore.
        </h2>
      </div>
      <div class="col-lg-6 order-1 order-lg-2 hero-img">
        <img src="assets/img/blog2.png" class="img-fluid animated mob-anim" alt="" />
      </div>
    </div>
  </div>
</section>
<!--- End Hero -->

<!-- -Blog Card Section -->

    <div id="blog" class="content" >
      <div class="container">
        <div class="row">
          <% for (let i = 0; i < 9 && i < blogs.length; i++) { %>
          <div class="col-xs-12 col-sm-4">
            <div class="card">
              <a
                class="img-card"
                href="/single?id=<%=blogs[i]._id%>"
              >
                <img
                <img src="data:<%= blogs[i].image.contentType %>;base64, <%= blogs[i].image.data.toString('base64') %>" alt="Blog Image">
              </a>
              <div class="card-content">
                <h4 class="card-title">
                  <a
                    href="/single?id=<%=blogs[i]._id%>"
                  >
                  <%=blogs[i].title%>
                  </a>
                </h4>
                <p class="">
                  <%=blogs[i].subject%>
                </p>
              </div>
              <div class="card-read-more">
                <a
                  href="/single?id=<%=blogs[i]._id%>"
                  class="btn btn-link btn-block"
                >
                  Read More
                </a>
              </div>
            </div>
          </div>
          <% } %>
        </div>
      </div>
    </div>
  </div>
  <!-- Pagination Links -->
<div class="container-fluid" style="background:var(--p-color);
padding:20px;
margin:0px !important">

  <div class="pagination d-flex justify-content-center align-items-center mt-4 mb-4">
    <% if (totalBlogs > pageSize) { %>
      <% if (currentPage > 1) { %>
        <a href="/blog?page=<%= currentPage - 1 %>" class="transparent-button" >PREVIOUS</a>
      <% } %>
      <% if (currentPage * pageSize < totalBlogs) { %>
        <a href="/blog?page=<%= currentPage + 1 %>" class="transparent-button">NEXT</a>
      <% } %>
    <% } %>
  </div>
</section>



</div>
<!-- -Blog Card Section -->
<%- include('footer'); -%>
